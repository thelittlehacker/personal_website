import CTAButton from "@/components/ui/CTAButton";
export default function PageHero({ title, subtitle, ctaText = "Book a Strategy Call", ctaHref = "/contact" }: { title: string; subtitle: string; ctaText?: string; ctaHref?: string }) { return (<section className="border-b border-slate-200 bg-white"><div className="container-wrap py-16 sm:py-20"><h1 className="max-w-3xl text-3xl font-bold text-slate-900 sm:text-5xl">{title}</h1><p className="mt-5 max-w-3xl text-lg text-slate-600">{subtitle}</p><div className="mt-8"><CTAButton href={ctaHref}>{ctaText}</CTAButton></div></div></section>); }
